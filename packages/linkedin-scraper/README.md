# LinkedIn Competitor Scraper

Transport-free Python helper. Outputs JSON, no direct Telegram sending.

## Usage

```bash
python3 linkedin-competitor-scraper.py --pick-count 3 --download-images --save-json output.json
```

Scheduled via Telegram bot `/task` using `linkedin-post-creator` skill.
